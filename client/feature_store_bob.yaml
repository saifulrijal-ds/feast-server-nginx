# ── CLIENT feature_store.yaml — bob (collection_officer) ───────
# bob can read customer_behavior_stats only.
# Attempts to access customer_credit_stats will raise PermissionError.
#
# HOW TO USE:
#   export FEAST_SERVER_HOST=ec2-xx-xx-xx-xx.compute-1.amazonaws.com
#   export FEAST_CERT_PATH=/home/ds/.feast/ca.crt
#   envsubst < feature_store_bob.yaml > feature_store.yaml
#   python test_rbac.py

project: credit_risk_modeling
entity_key_serialization_version: 3

registry:
  registry_type: remote
  path: ${FEAST_SERVER_HOST}:443
  cert: ${FEAST_CERT_PATH}

offline_store:
  type: remote
  host: ${FEAST_SERVER_HOST}
  port: 443
  scheme: https
  cert: ${FEAST_CERT_PATH}

online_store:
  type: remote
  path: https://${FEAST_SERVER_HOST}:443
  cert: ${FEAST_CERT_PATH}

auth:
  type: oidc
  client_id: feast-app
  client_secret: feast-secret
  username: bob
  password: password123
  auth_discovery_url: https://${FEAST_SERVER_HOST}/realms/feast-realm/.well-known/openid-configuration
  verify_ssl: false
